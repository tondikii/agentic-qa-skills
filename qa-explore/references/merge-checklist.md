# qa-explore: merge checklist

When `qa/app-map.md` already exists, apply every row before writing:

- [ ] New modules and submenus appended; existing entries untouched.
- [ ] `visible-to` on every touched entry includes this persona.
- [ ] Persona-coverage table gains or updates this persona's row.
- [ ] Suspected gaps updated: resolved entries removed, new unobserved items added.
- [ ] Completeness lists this persona; global claim only if all known personas are covered.
- [ ] No secret values anywhere in the file (env var names only).
